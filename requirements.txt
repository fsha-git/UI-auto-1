# 只锁直接依赖，传递依赖交给 pip 解析。
#
# playwright 本来是 pytest-playwright 的传递依赖，这里显式列出并锁死，是因为
# docker/Dockerfile 的基础镜像 tag（mcr.microsoft.com/playwright/python:v1.62.0-*）
# 自带的是这一版对应的浏览器构建；两边版本一旦漂开，pip 装的 Playwright 就会去找
# 镜像里不存在的 /ms-playwright/chromium-<build> 目录。改这一行必须同步改 Dockerfile。
playwright==1.62.0

pytest==9.1.1
pytest-playwright==0.9.0
pytest-bdd==8.1.0
pytest-reportlog==1.0.0
pytest-rerunfailures==16.6
pytest-timeout==2.4.0
pytest-html==4.2.0
pytest-cov==7.1.0
